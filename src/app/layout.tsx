import type { Metadata } from "next";
import { Plus_Jakarta_Sans, Inter } from "next/font/google";
import "./globals.css";

const plusJakartaSans = Plus_Jakarta_Sans({
  subsets: ["latin"],
  variable: "--font-jakarta",
  weight: ["400", "500", "600", "700", "800"],
  style: ["normal", "italic"],
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  weight: ["400", "500", "600", "700"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Pixtalase — Your Brand's Website, Handled.",
  description:
    "Pixtalase helps growing brands build a professional website presence from setup to launch and beyond. Web development, hosting, SEO, maintenance — all in one plan.",
  openGraph: {
    title: "Pixtalase — Your Brand's Website, Handled.",
    description:
      "Selling well but margins feel thin? It's time to have your own website. Pixtalase handles design, development, and maintenance so you can focus on your brand.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${plusJakartaSans.variable} ${inter.variable}`}>
      <head>
        {/* eslint-disable-next-line @next/next/no-sync-scripts */}
        <script src="https://mcp.figma.com/mcp/html-to-design/capture.js" async></script>
      </head>
      <body>{children}</body>
    </html>
  );
}
